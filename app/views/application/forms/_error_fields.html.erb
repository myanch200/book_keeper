<% if f.object.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(f.object.errors.count, "error") %> prohibited this <%= f.object.name %> from being saved:</h2>

      <ul>
        <% f.object.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
